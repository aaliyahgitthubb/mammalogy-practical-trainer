Mammalogy GitHub Update 1.8.9

Fixes Google Photos Picker selection timing behavior. The Vault now checks the session immediately, uses Google’s polling configuration, re-checks when you return to the tab, removes the artificial 10-minute cap, and cleans up completed/expired sessions.

Preserved user data: data.js, embedded_images.js, and images/ are intentionally NOT included. Extract this ZIP before uploading the included files to the repository root.
